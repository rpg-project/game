{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Listes des {{ function }}</h1>
        <ul>
            {% for quest in quests %}
                <li>Chapitre {{ quest.difficulty }} : {{ quest.title }} {% block done %}{{ render(controller(
                    'AppBundle\\Controller\\PlaceController::questsDone', {'id' : quest.id})) }}{% endblock %}</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}