{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1> {{ function.name }} </h1>
            <ul>
                <li><a href="{{ path("summons", {'level' : place.level, 'placeType' : place.type, 'summonType': 1 }) }}">Invocation Simple 10 {{ money }}</a></li>
                <li><a href="{{ path("summons", {'level' : place.level, 'placeType' : place.type, 'summonType': 10 }) }}">Multi-Invocation 100 {{ money }}</a></li>
            </ul>
        </div>
    </div>

{% endblock %}